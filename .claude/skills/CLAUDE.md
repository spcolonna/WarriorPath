# Instrucciones Globales para Arena Project
- Nunca explores ni leas archivos dentro de las carpetas `build/` o `.dart_tool/`.
- Prioriza el uso de archivos en `lib/` para cualquier modificación de lógica.